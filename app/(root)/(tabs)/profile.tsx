import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl font-bold text-blue-500">Profile</Text>
    </SafeAreaView>
  );
};
export default Profile;
