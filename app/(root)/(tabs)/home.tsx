import { Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl font-bold text-blue-500">Home</Text>
    </SafeAreaView>
  );
};
export default Home;
