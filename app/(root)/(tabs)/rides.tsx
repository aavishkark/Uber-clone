import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl font-bold text-blue-500">History</Text>
    </SafeAreaView>
  );
};
export default Rides;
